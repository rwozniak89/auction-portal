import { FilterAuctionPipe } from './filter-auction.pipe';

describe('FilterAuctionPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterAuctionPipe();
    expect(pipe).toBeTruthy();
  });
});
